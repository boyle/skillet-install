User
====
- load ssh key (from backup?)
- load gpg key (from aboyle.ca?)
- setup virtual machines
- setup work repos
- bash powerline
- vim powerline
- pull down EIDORS
   \$ cd ~/proj && git svn clone svn+ssh://alistair_boyle@svn.code.sf.net/p/eidors3d/code/trunk eidors3d.git
- install matlab
   \$ cd ~/bin && ln -sf /opt/matlab/r2017a/bin/matlab
- install netgen
- install the eidors wrapper
   \$ scp aboyle.ca:~/bin/eidors ~/bin/
- get most recent repositories (depot)


System
======
- setup restic (backup)
- netgen
  maybe...
   $ sudo apt install libxmu-dev mesa-common-dev libglu1-mesa-dev libx11-dev python3-dev
   $ cd ~/proj/eidors3d.git/dev/a_boyle && ./install-netgen-6.0-beta
- gmsh
- blender
- remove snapd
- setup printer, see printer.txt
   \$ scp aboyle.ca:~/printer.txt

Not Automated
=============
- pair Signal with phone
- add ssh authorized_keys to grendel (post-install)
- Nextcloud client config
- KeepassXC config
- Teams config
- SSH keys to sourceforge
- SSH keys to github
- SSH keys to work
